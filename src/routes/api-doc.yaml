paths:
  # ------------------------------
  # AUTHORIZE
  # ------------------------------
  /auth/generate:
    post:
      tags:
        - Auth
      summary: Prueba de generacion de JWT.
      description: Prueba para la validación de la generación de Token JWT.
      requestBody:
        description: Crea un modelo de referencia del token. 
        content:
          application/json:
            schema:
              type: object
              properties:
                id: 
                  type: integer
                  example: 1234
                username: 
                  type: string
                  example: ABCDEFJ
        required: true
      responses:
        '200': 
          description: 'JSON con los datos ingresados y el token'
          content:
            application/json:
              schema:
                type: object
                properties:
                  authId:
                    type: integer
                    example: 1234
                  authUser:
                    type: string
                    example: ABCDEFJ
                  token:
                    type: string
                    example: A.B.C
        '400':
          description: Fallo al generar el token.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 500
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Token no generado.
        '500':
          $ref: '#/components/responses/ServerError'
      deprecated: true
      security: []
  /auth/register:
    post:
      tags:
        - Auth
      summary: Registra un nuevo usuario.
      description: Inserta un nuevo usuario en la base de datos.
      requestBody:
        description: Registra un nuevo usuario. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRequest'
        required: true
      responses:
        '201':
          description: Respuesta exitosa al registrarse el usuario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 201
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Usuario registrado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '500':
          $ref: '#/components/responses/ServerError'
      security: []
  /auth/login:
    post:
      tags:
        - Auth
      summary: Inicia una sesión.
      description: Valida las credenciales y genera un token de acceso.
      requestBody:
        description: Registra un nuevo usuario. 
        content:
          application/json:
            schema:
              type: object
              properties:
                username:
                  type: string
                  description: Nombre de usuario
                  example: username1
                contraseña:
                  type: string
                  format: password
                  description: Contraseña
                  example: 12345
        required: true
      responses:
        '201':
          description: Respuesta exitosa al verificarse las credenciales de acceso.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 201
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Usuario y Token verificados.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          description: Fallo al validar las credenciales del usuario
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 401
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Credenciales inválidas.
        '500':
          $ref: '#/components/responses/ServerError'
      security: []
  /auth/logout:
    post:
      tags:
        - Auth
      summary: Cierra la sesión.
      description: Elimina los valores de la sesión, incluyendo el token.
      responses:
        '200':
          description: Cierre de sesión exitoso.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: 
                    type: string
                    example: Cierre de sesión exitoso.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  # ------------------------------
  # USERS
  # ------------------------------
  /users:
    get:
      tags:
        - Users
      summary: Obtiene una lista de los usuarios.
      description: Recupere una lista de usuarios de la base de datos.
      responses:
        '200':
          description: Respuesta exitosa con una lista de usuarios.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Se listo correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags:
        - Users
      summary: Crea un nuevo usuario.
      description: Inserta un nuevo usuario en la base de datos.
      requestBody:
        description: Crea un nuevo usuario. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRequest'
        required: true
      responses:
        '201':
          description: Respuesta exitosa al crearse el usuario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 201
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Usuario creado correctamente!
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  /users/{id}:
    get:
      tags:
        - Users
      summary: Obtiene al usuario por su UUID.
      description:  Recupere al usuario especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramUUID'  
      responses:
        '200':
          description: Respuesta exitosa con la información del usuario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Usuario obtenido correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del usuario no es válido
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Usuario no encontrado
        '500':
          $ref: '#/components/responses/ServerError'  
    patch:
      tags:
        - Users
      summary: Actualiza el usuario.
      description: Actualiza la información del usuario especificado.
      parameters:
        - $ref: '#/components/parameters/paramUUID'  
      requestBody:
        description: Crea un nuevo usuario. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRequest'
        required: false
      responses:
        '200':
          description: Respuesta exitosa con la información del usuario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Usuario actualizado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del usuario no es válido
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Usuario no encontrado
        '500':
          $ref: '#/components/responses/ServerError'   
    delete:
      tags:
        - Users 
      summary: Elimina al usuario.
      description:  Elimina al usuario especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramUUID'  
      responses:
        '200':
          description: Respuesta exitosa con la confirmación del evento.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Usuario eliminado correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: ID del usuario no es válido
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Usuario no encontrado
        '500':
          $ref: '#/components/responses/ServerError'
  /users/{id}/chats:
    get:
      tags:
        - Chats
      summary: Obtiene los chats del usuario.
      description:  Recupere los chats donde el usuario especificado es miembro.
      parameters:
        - $ref: '#/components/parameters/paramUUID'  
      responses:
        '200':
          description: Respuesta exitosa con una lista de los chats donde el usuario es miembro.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Chats obtenidos correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: No es miembro de algun chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: No tiene chats.
        '500':
          $ref: '#/components/responses/ServerError'
  # ------------------------------
  # CHATS
  # ------------------------------
  /chats:
    get:
      tags:
        - Chats
      summary: Obtiene una lista de los chats.
      description: Recupere una lista de chats de la base de datos.
      responses:
        '200':
          description: Respuesta exitosa con una lista de chats.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Chats obtenidos correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags:
        - Chats
      summary: Crea un nuevo chat.
      description: Inserta un nuevo chat en la base de datos.
      requestBody:
        description: Crea un nuevo chat. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
        required: true
      responses:
        '201':
          description: Respuesta exitosa al crearse el chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 201
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Chat creado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  /chats/{id}:
    get:
      tags: 
        - Chats
      summary: Obtiene un chat por su ID.
      description: Recupere el chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la información del Chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Chat obtenido correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del chat no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: El chat no existe.
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: 
        - Chats
      summary: Actualiza el chat.
      description: Actualiza la información del chat especificado.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Crea un nuevo chat. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
        required: true
      responses:
        '200':
          description: Respuesta exitosa con la información del Chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ChatResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Chat actualizado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del chat no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Chat no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags:
        - Chats
      summary: Elimina el chat.
      description: Elimina el chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la confirmación del evento.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Chat eliminado correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: ID del chat no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Chat no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
  # ------------------------------
  # MEMBERS
  # ------------------------------
  /chats/{id}/members:
    get:
      tags: 
        - Members
      summary: Obtiene los miembros del chat.
      description: Recupere los miembros del chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la lista de miembros del chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MemberResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Miembros obtenidos correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del miembro no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: El chat no tiene miembros.
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: 
        - Members
      summary: Registra un nuevo miembro al chat.
      description: Registra un nuevo miembro al chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Agrega un nuevo miembro al chat. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemberRequest'
      responses:
        '200':
          description: Respuesta exitosa con la lista de miembros del chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MemberResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Miembros obtenidos correctamente
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del miembro no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Miembro no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
  /chats/{id}/members/{user}:
    delete: 
      tags: 
        - Members
      summary: Elimina al miembro del chat.
      description: Elimina al miembro especificado del chat.
      parameters:
        - $ref: '#/components/parameters/paramID'  
        - $ref: '#/components/parameters/paramUUID2'  
      responses:
        '200':
          description: Respuesta exitosa con la confirmación del evento.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Miembro eliminado correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: ID del miembro no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Miembro no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
  # ------------------------------
  # MESSAGES
  # ------------------------------
  /chats/{id}/messages:
    get:
      tags: 
        - Messages
      summary: Obtiene los mensajes del chat.
      description: Recupere los mensajes del chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la lista de los mensajes del chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MessageResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Mensajes obtenidos correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del chat no es válido o el chat no cuenta con mensajes activos.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Mensajes no encontrados.
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: 
        - Messages
      summary: Agrega un nuevo mensaje al chat.
      description: Registra un nuevo mensaje al chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Agrega un nuevo mensaje al chat. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessageRequest1'
      responses:
        '201':
          description: Respuesta exitosa con la lista de los mensajes del chat.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MessageResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Mensaje creado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  /messages:
    get:
      tags: 
        - Messages
      summary: Obtiene una lista de los mensajes.
      description: Recupere una lista de todos los mensajes de la base de datos.
      responses:
        '200':
          description: Respuesta exitosa con una lista de los mensajes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MessageResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Mensajes obtenidos correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  /messages/{id}:
    get:
      tags: 
        - Messages
      summary: Obtiene un mensaje por su ID.
      description: Recupere el mensaje especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la información del mensaje.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MessageResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Mensaje obtenido correctamente
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del mensaje no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Mensaje no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: 
        - Messages
      summary: Actualiza un mensaje.
      description: Actualiza la información del mensaje especificado.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Crea un nuevo chat. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MessageRequest2'
        required: true
      responses:
        '200':
          description: Respuesta exitosa con la información del mensaje.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MessageResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Mensaje actualizado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del mensaje no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Mensaje no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags:
        - Messages
      summary: Elimina el chat.
      description: Elimina el chat especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la confirmación del evento.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Mensaje eliminado correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: ID del mensaje no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Mensaje no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
  # ------------------------------
  # POSTS
  # ------------------------------
  /publications:
    get:
      tags: 
        - Posts
      summary: Obtiene una lista de las publicaciones.
      description: Recupere una lista de todas las publicaciones de la base de datos.
      responses:
        '200':
          description: Respuesta exitosa con una lista de las publicaciones.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PostResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Publicaciones obtenidas correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags:
        - Posts
      summary: Crea una nueva publicación.
      description: Registra una nueva publicación en la base de datos.
      requestBody:
        description: Crea una nueva publicación.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PostRequest'
      responses:
        '201':
          description: Respuesta exitosa con la publicación creada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PostResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Publicación creada correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  /publications/{id}:
    get:
      tags: 
        - Posts
      summary: Obtiene la publicación especificada.
      description: Recupere información de la publicación especificada de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la información de la publicación.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PostResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Publicación obtenida correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID de la publicación no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: La publicación no existe.
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: 
        - Posts
      summary: Actualiza la publicación.
      description: Actualiza la información de la publicación especificada.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Actualiza la publicación. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PostRequest2'
        required: true
      responses:
        '200':
          description: Respuesta exitosa con la información de la publicación.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PostResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Publicación actualizada correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID de la publicación no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: La publicación no existe.
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags:
        - Posts 
      summary: Elimina la publicación.
      description:  Elimina la publicación especificada de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la confirmación del evento.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Publicación eliminada correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: ID de la publicación no es válida.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Publicación no encontrada.
        '500':
          $ref: '#/components/responses/ServerError'
  # ------------------------------
  # COMMENTS
  # ------------------------------
  /publications/{id}/comments:
    get:
      tags:
        - Comments
      summary: Obtiene una lista de los comentarios de la publicación.
      description: Recupere una lista de todos los comentarios de la publicación.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con una lista de comentarios de la publicación.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CommentsResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Comentarios obtenidos correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags:
        - Comments
      summary: Crea un nuevo comentario.
      description: Agrega un nuevo comentario a la publicación especificada.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Crea un nuevo comentario. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommentsRequest'
        required: true
      responses:
        '201':
          description: Respuesta exitosa con el comentario creado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 201
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CommentsResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Comentario creado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/ServerError'
  /comments/{id}:
    get:
      tags: 
        - Comments
      summary: Obtiene el comentario especificado.
      description: Recupere información del comentario especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con la información del comentario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CommentsResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Comentario obtenida correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del comentario no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Comentario no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags:
        - Comments
      summary: Actualiza el comentario.
      description: Actualiza la información del comentario especificado.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      requestBody:
        description: Actualiza el comentario. 
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommentsRequest2'
        required: true
      responses:
        '200':
          description: Respuesta exitosa con la información del comentario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CommentsResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Comentario actualizado correctamente.
        '400':
          $ref: '#/components/responses/SchemaInvalid'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del comentario no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Comentario no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags:
        - Comments
      summary: Elimina el comentario.
      description:  Elimina el comentario especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses: 
        '200':
          description: Respuesta exitosa con la confirmación del evento.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Comentario eliminada correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: ID del comentario no es válida.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Comentario no encontrada.
        '500':
          $ref: '#/components/responses/ServerError'
  /comments/{id}/answers:
    get:
      tags:
        - Comments
      summary: Obtiene las respuestas del comentario.
      description:  Recupera una lista de las respuestas del comenterio especificado de la base de datos.
      parameters:
        - $ref: '#/components/parameters/paramID'  
      responses:
        '200':
          description: Respuesta exitosa con una lista de respuestas del comentario especificado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: success
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 200
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CommentsResponse'
                  message:
                    type: string
                    description: Mensaje que describe el resultado.
                    example: Respuestas obtenidas correctamente.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: ID del comentario no es válido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Estado de la respuesta.
                    example: error
                  code:
                    type: integer
                    description: Codigo de estado HTTP.
                    example: 404
                  data:
                    type: object
                    properties: 
                      details:
                        oneOf:
                          - type: string
                            example: null
                          - type: array
                            example: []
                  message:
                    type: string
                    description: Mensaje que describe el resultado
                    example: Comentario no encontrado.
        '500':
          $ref: '#/components/responses/ServerError'

